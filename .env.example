# =============================================================================
# ARCO 환경 변수 템플릿
# =============================================================================
# 이 파일은 템플릿입니다. 실제 환경별 설정은 아래를 참고하세요.
# =============================================================================

# =============================================================================
# 🔵 DEVELOPMENT (로컬 개발 환경)
# =============================================================================
# 파일: .env.local (로컬 컴퓨터)
# 
# 테스트용 리소스를 사용합니다:
# - Supabase: acro-db-test (또는 기존 DB)
# - R2: acro-store-test (또는 기존 버킷)
# - Domain: http://localhost:3000
# =============================================================================

# Supabase (테스트 DB)
NEXT_PUBLIC_SUPABASE_URL=https://your-test-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-test-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key

# Cloudflare R2 (테스트 버킷)
CLOUDFLARE_ACCOUNT_ID=your-account-id
CLOUDFLARE_R2_ACCESS_KEY_ID=your-access-key-id
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your-secret-access-key
CLOUDFLARE_R2_BUCKET_NAME=acro-store-test
CLOUDFLARE_R2_PUBLIC_URL=https://pub-test-xxxxx.r2.dev

# Cloudflare Stream (테스트 토큰)
CLOUDFLARE_STREAM_API_TOKEN=your-test-stream-token

# Toss Payments (테스트 키)
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_xxxxx
TOSS_SECRET_KEY=test_sk_xxxxx

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_EMAIL=admin@arco.com

# Email (선택)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password


# =============================================================================
# 🟢 PRODUCTION (운영 환경)
# =============================================================================
# 설정 위치: Vercel Dashboard → Settings → Environment Variables
# Environment: Production만 선택
# 
# 운영용 리소스를 사용합니다:
# - Supabase: acro-db-prod
# - R2: acro-store-prod
# - Domain: https://arco.kr
# =============================================================================

# Supabase (운영 DB) - Vercel Production에만 설정
# NEXT_PUBLIC_SUPABASE_URL=https://acro-db-prod.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-prod-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-prod-service-role-key

# Cloudflare R2 (운영 버킷) - Vercel Production에만 설정
# CLOUDFLARE_ACCOUNT_ID=your-account-id
# CLOUDFLARE_R2_ACCESS_KEY_ID=your-prod-access-key-id
# CLOUDFLARE_R2_SECRET_ACCESS_KEY=your-prod-secret-access-key
# CLOUDFLARE_R2_BUCKET_NAME=acro-store-prod
# CLOUDFLARE_R2_PUBLIC_URL=https://pub-prod-xxxxx.r2.dev

# Cloudflare Stream (운영 토큰) - Vercel Production에만 설정
# CLOUDFLARE_STREAM_API_TOKEN=your-prod-stream-token

# Toss Payments (실제 키) - Vercel Production에만 설정
# NEXT_PUBLIC_TOSS_CLIENT_KEY=live_ck_xxxxx
# TOSS_SECRET_KEY=live_sk_xxxxx

# App - Vercel Production에만 설정
# NEXT_PUBLIC_APP_URL=https://arco.kr
# NEXT_PUBLIC_ADMIN_EMAIL=admin@arco.com


# =============================================================================
# 🟡 PREVIEW (Vercel 프리뷰 환경)
# =============================================================================
# 설정 위치: Vercel Dashboard → Settings → Environment Variables
# Environment: Preview만 선택
# 
# 테스트 리소스를 사용합니다 (Development와 동일):
# - Supabase: acro-db-test
# - R2: acro-store-test
# - Domain: https://preview-xxx.vercel.app
# =============================================================================

# Supabase (테스트 DB) - Vercel Preview에만 설정
# NEXT_PUBLIC_SUPABASE_URL=https://your-test-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-test-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key

# Cloudflare R2 (테스트 버킷) - Vercel Preview에만 설정
# CLOUDFLARE_R2_BUCKET_NAME=acro-store-test
# CLOUDFLARE_R2_PUBLIC_URL=https://pub-test-xxxxx.r2.dev

# App - Vercel Preview에만 설정
# NEXT_PUBLIC_APP_URL=자동 생성됨 (Vercel이 제공)


# =============================================================================
# 📋 환경별 설정 요약
# =============================================================================
#
# ┌──────────────┬────────────────────┬───────────────────┬──────────────────┐
# │ 환경         │ Supabase           │ R2 Bucket         │ 도메인           │
# ├──────────────┼────────────────────┼───────────────────┼──────────────────┤
# │ Development  │ acro-db-test       │ acro-store-test   │ localhost:3000   │
# │ Preview      │ acro-db-test       │ acro-store-test   │ preview-xxx...   │
# │ Production   │ acro-db-prod       │ acro-store-prod   │ arco.kr          │
# └──────────────┴────────────────────┴───────────────────┴──────────────────┘
#
# =============================================================================


# =============================================================================
# 🔧 로컬 개발 시작하기
# =============================================================================
#
# 1. 이 파일을 복사:
#    cp .env.example .env.local
#
# 2. .env.local에서 Development 섹션의 주석(#)을 제거하고 실제 값 입력
#
# 3. 개발 서버 시작:
#    npm run dev
#
# =============================================================================


# =============================================================================
# 🚀 Vercel 배포 설정하기
# =============================================================================
#
# 1. Vercel Dashboard 접속
#    https://vercel.com/dashboard
#
# 2. 프로젝트 선택 → Settings → Environment Variables
#
# 3. Production 환경 변수 추가:
#    - Environment: Production만 체크
#    - 위 Production 섹션의 변수들 입력
#
# 4. Preview 환경 변수 추가:
#    - Environment: Preview만 체크
#    - 위 Preview 섹션의 변수들 입력
#
# =============================================================================
